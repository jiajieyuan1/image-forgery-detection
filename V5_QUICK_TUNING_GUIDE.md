# v5版本 - 快速参数调整参考

## 关键参数位置

文件：`src/inference.py`
方法：`infer_single_image()`

## 可直接调整的参数

### 🎯 第一步：如果前两张还是没被检测出来

**问题**：前两张图片仍然被判为真实
**原因**：可能fake_prob < 0.50

**解决方案**：继续降低中等阈值

```python
# 第二阶段，找到这一行（约第95-100行）：
elif fake_prob >= 0.50 and prob_diff >= 0.10:

# 改为：
elif fake_prob >= 0.48 and prob_diff >= 0.08:
```

**同时改低置信度阈值**：
```python
# 找到第三阶段这一行（约第110-115行）：
elif fake_prob >= 0.52:

# 改为：
elif fake_prob >= 0.50:
```

### 🎯 第二步：如果前两张被检测出来，但真实图片也被误判为AI

**问题**：某些真实图片被错判为AI
**原因**：参数太敏感

**解决方案**：提高阈值

```python
# 第二阶段：
elif fake_prob >= 0.50 and prob_diff >= 0.10:

# 改为：
elif fake_prob >= 0.52 and prob_diff >= 0.12:
```

**同时提高低置信度**：
```python
# 第三阶段：
elif fake_prob >= 0.52:

# 改为：
elif fake_prob >= 0.54:
```

## 参数调整速查表

### 中等阈值（fake_prob的临界值）

| 值 | 敏感度 | 适用情况 |
|----|--------|--------|
| 0.45 | 超级激进 | AI漏报严重 |
| 0.48 | 很激进 | AI仍有漏报 |
| **0.50** | **激进** | **v5默认，适合您的图片** |
| 0.52 | 平衡 | 开始担心误报 |
| 0.55 | 保守 | v4原始版本 |
| 0.60 | 超级保守 | 只信任高置信度 |

### 差距要求（真假概率差距）

| 值 | 敏感度 | 适用情况 |
|----|--------|--------|
| 0.05 | 超级激进 | 很容易被判AI |
| 0.08 | 激进 | 差距不明显也判AI |
| **0.10** | **平衡激进** | **v5默认** |
| 0.12 | 平衡 | 需要些许分离 |
| 0.15 | 保守 | v4原始版本 |
| 0.20 | 超级保守 | 需要明显分离 |

### 低置信度阈值

| 值 | 敏感度 | 说明 |
|----|--------|------|
| 0.50 | 超级激进 | 只要fake > real就判AI |
| **0.52** | **激进** | **v5默认** |
| 0.54 | 平衡 | 中等敏感 |
| 0.55 | 保守 | v4原始版本 |
| 0.58 | 超保守 | 很少这么低的fake会被判 |

## 调整组合建议

### 🟢 组合A：激进检测（推荐尝试第一步）
**目标**：更好地检测您的AI图片，接受一定误报
```python
# 第二阶段
elif fake_prob >= 0.48 and prob_diff >= 0.08:

# 第三阶段  
elif fake_prob >= 0.50:
```

### 🟡 组合B：平衡（如果A导致太多误报）
**目标**：检测和准确性平衡
```python
# 第二阶段
elif fake_prob >= 0.50 and prob_diff >= 0.10:

# 第三阶段
elif fake_prob >= 0.52:
```
✅ 这是当前v5的默认配置

### 🔴 组合C：保守（如果误报太多）
**目标**：优先准确性，接受某些AI漏报
```python
# 第二阶段
elif fake_prob >= 0.52 and prob_diff >= 0.12:

# 第三阶段
elif fake_prob >= 0.54:
```

## 步骤指引

### 第1次测试（使用v5默认参数）
```bash
python test_improved_detection.py --dir ai图片
```
记录：前两张是否现在被判为AI？

### 第2次测试（如果前两张还是没检测出）
修改参数到【组合A】，重新运行测试

### 第3次测试（如果开始有误报）
修改参数到【组合C】，或回到【组合B】

## 记录您的调整过程

建议填写此表格跟踪：

```
测试1 (v5默认): 
  - 第1-2张: [检测结果]
  - 第3-4张: [检测结果]
  - 总体准确: [%]

测试2 (组合A):
  - 第1-2张: [检测结果]
  - 第3-4张: [检测结果]
  - 总体准确: [%]

测试3 (组合B或C):
  - ...
```

## 最简单的调整方法

如果觉得复杂，按以下顺序尝试：

```bash
# 1. 先测试v5默认（已经改好了）
python test_improved_detection.py --dir ai图片

# 2. 如果前两张还是没检测出，改这一行（最重要）：
# src/inference.py 第95行从 0.50 改为 0.48

# 3. 重新测试
python test_improved_detection.py --dir ai图片

# 4. 如果有误报或准确率下降，改这一行：
# src/inference.py 第95行从 0.48 改为 0.50（改回）
```

## 在线帮助

遇到问题时告诉我：
- 前两张检测结果
- 后两张检测结果  
- 想要的效果

我可以直接给您最优的参数组合！
