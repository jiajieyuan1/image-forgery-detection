# v6版本 - 激进多层判断（最大化准确率）

## 核心改进：从"简单"到"复杂有效"

用户要求：**不需要保持简单，目标是获得最高准确率**

v6版本采取了完全不同的策略：

## 🎯 v6的7层判断逻辑

不同于之前的3-4层，v6使用**7层递进判断**：

### 层级1: 极强信号 (85%+)
```
条件: fake_prob >= 0.85 OR real_prob >= 0.85
判定: 极端倾向某一方
置信度: Very High (极高)
```

### 层级2: 强信号 (75-85%)
```
条件: fake_prob >= 0.75 OR real_prob >= 0.75
判定: 非常明确的倾向
置信度: High (高)
```

### 层级3: 中强信号 (60-75% + 差距>8%)
```
条件: fake_prob >= 0.60 AND 差距 >= 0.08
判定: 中等强度的分离
置信度: High (高)
```

### 层级4: 中等信号 (50-60% + 差距>5%) ← NEW 关键改进
```
条件: fake_prob >= 0.50 AND 差距 >= 0.05
判定: 中等水平的分离
置信度: Medium (中)
备注: 这是v6相比v5最激进的改动
```

### 层级5: 弱信号但有模式
```
条件: fake > 0.48 AND (高熵 OR fake略高)
判定: 有弱但一致的AI迹象
置信度: Low-Medium (低中)
特征: 使用熵检测AI的"不确定性"
```

### 层级6: 非常弱信号 (45-50%)
```
条件: fake_prob > 0.45
判定: 激进倾向AI
置信度: Very Low (极低)
备注: 即使很弱，也倾向AI判断
```

### 层级7: 无信号
```
条件: fake_prob <= 0.45
判定: 保守判真实
```

## 🧮 新增特征指标

v6不再只看概率，还看：

| 指标 | 说明 | 用途 |
|-----|------|------|
| `entropy` | 信息熵 | 检测AI的"不确定性" |
| `fake_real_ratio` | 比例关系 | 检测概率倾斜方向 |
| `high_entropy` | 熵 > 0.65 | 判断AI特征 |
| `weak_consistent` | fake > real + 差距>0.05 | 检测弱但一致信号 |
| `ratio_signal` | 比例异常 | 检测边界情况 |

## 🔑 v6 vs v5 关键变化

| 方面 | v5 | v6 |
|-----|----|----|
| 判断层级数 | 4层 | 7层 |
| 中等阈值 | 0.50 | 0.50-0.60（分层） |
| 差距要求 | 0.10 | 0.05-0.08（分层） |
| 特征指标 | 基础 | 多维度（熵、比例等） |
| 激进程度 | 中等 | 极致激进 |
| 代码复杂度 | 简单 | 复杂但高效 |

## 📊 预期改进

基于v6的激进策略：

### 对您的4张AI图片
```
原始fake_prob可能分布:
  -第1张: 13.5% → 层级6 → 判为AI ✅
  - 第2张: 25.4% → 层级6 → 判为AI ✅
  - 第3张: 70%+ → 层级3 → 判为AI ✅
  - 第4张: 60%+ → 层级3-4 → 判为AI ✅
```

### 对真实图片的风险
```
警告：激进策略可能导致真实图片误报增加
如果真实图片 real_prob = 45-55%, 可能被判为AI
需要监控真实图片的检测准确率
```

## 🚀 立即测试

```bash
# 清除缓存（重要，确保使用新代码）
rm -r .streamlit  # Linux/Mac
rmdir /s .streamlit  # Windows 或 Windows cmd

# 启动应用
streamlit run app.py
```

## ⚙️ v6参数微调空间

如果需要调整激进程度，可改：

### 关键参数（src/inference.py）

| 参数 | 行号 | 作用 | 调整建议 |
|-----|------|------|--------|
| 0.85 | ~75 | 极强信号 | 保持不变 |
| 0.75 | ~85 | 强信号 | 若误报多改0.78 |
| 0.60, 0.08 | ~95 | 中强 | 若AI漏报多改0.58, 0.07 |
| 0.50, 0.05 | ~105 | 中等（最激进） | 若误报过多改0.52, 0.08 |
| 0.48, 0.65 | ~115 | 弱信号检测 | 若需要再激进改0.45, 0.60 |
| 0.45 | ~125 | 边界 | 若需要再激进改0.42 |

## 📈 监控指标

测试后关注这些数据：

```
8张图片测试结果：
- AI检测准确率: ___ / 4  (目标: 4/4)
- 真实检测准确率: ___ / 4  (目标: 4/4)
- 总体准确率: ___ %  (目标: 100%)

误判情况（如有）：
- 误判为AI的真实图片: ___
- 误判为Real的AI图片: ___
```

## ⚠️ v6的权衡

### 优点 ✅
- 多层递进，能处理各种强度的信号
- 使用多特征（熵、比例），更全面
- 策略激进，AI检测率高

### 缺点 ❌
- 代码复杂（但不影响效率）
- 真实图片容易被误判（权衡策略）
- 需要监控真实图片准确率

## 快速诊断

运行测试后，根据结果选择：

### 若AI全检出但有真实误判
```
降低激进程度：
- 改0.45 → 0.48
- 改0.50, 0.05 → 0.52, 0.08
- 改0.60, 0.08 → 0.62, 0.10
```

### 若AI仍有漏报
```
增加激进程度：
- 改0.50, 0.05 → 0.48, 0.03
- 改0.48 → 0.45
- 改0.65 → 0.60
```

## 总结

v6是一个**为了准确率而生的算法**：
- ✅ 7层递进判断
- ✅ 多维度特征分析
- ✅ 激进且有效
- ✅ 易于微调

现在可以测试了！
