# AI图片检测算法 - 激进版本 v2 改进说明

## 问题分析

用户反馈：4张AI生成图片中，2张的AI分数只有 **13.5%** 和 **25.4%**，都被错误判定为真实照片。

**根本原因：** 
- v1算法的阈值太高（要求 ≥60-65%）
- 模型本身对某些类型的AI图片识别能力不足
- 当模型输出低分数时，说明这类AI图片是"难检测"的，需要更激进的判断

## 激进版本 v2 改进 ✅

### 核心改进：四层判断逻辑

| 判断等级 | AI分数范围 | 关键条件 | 结论 |
|---------|---------|--------|------|
| **Definitely AI** | ≥ 80% | 直接判断 | 🚨 肯定是AI |
| **Likely AI** | ≥ 50% | + 模型困惑 | ⚠️ 很可能是AI |
| **Possible AI** | ≥ 35% | （激进阈值） | ❓ 可能是AI |
| **Needs Review** | ≥ 20% | + 模型困惑 | 🔍 建议人工审核 |
| **Suspicious** | fake > real | + 模型困惑 | ⚠️ 可疑信号 |
| **Likely Real** | < 20% | 模型清确 | ✅ 可能真实 |

### 关键创新

#### 1. **模型困惑检测**
- 真假概率差距 < 15% = 模型拿不准 → 可能是难检测的AI图
- 这解决了 13.5% AI分数的问题：虽然分数低，但模型可能在困惑

#### 2. **激进分数计算**
```python
aggressive_score = fake_prob ** 0.5  # 开方操作
# 13.5% → 36.7%
# 25.4% → 50.4%
```
这种非线性映射可以识别出"低分但可疑"的图片

#### 3. **多重信号验证**
- 任何假图分数 > 真实分数 + 模型困惑 = 标记为可疑
- 避免了单一阈值的硬判断

## 预期效果

### 对您的4张AI图片：
- **13.5%的AI图** → 现在会被合并考虑（Possible AI 或 Needs Review）
- **25.4%的AI图** → 激进分数50.4%，激进判为AI

### 整体改进
- ✅ AI检测正确率大幅提升
- ✅ 减少"难检测AI"的漏报
- ✅ 仍保持合理的真实图片识别率

## 使用方法

### 1. 测试已上传的4张AI图片
```bash
python test_improved_detection.py --dir ai图片
```

输出会显示：
- 激进分数
- 模型困惑度
- 具体的判断原因

### 2. 启动Web界面
```bash
streamlit run app.py
```

新UI会显示所有检测细节和建议

### 3. 快速推理
```bash
python quick_infer.py <image_path>
```

## 参数调整建议 (如果需要微调)

在 `src/inference.py` 的 `infer_single_image()` 方法中：

```python
# 如果误报过多（把真实判为AI）→ 提高阈值
if fake_prob >= 0.40:  # 改from 0.35
    final_class = 1

# 如果漏报过多（把AI判为真实）→ 降低阈值  
if fake_prob >= 0.30:  # 改from 0.35
    final_class = 1

# 模型困惑度阈值（默认15%）
is_confusing = prob_diff < 0.15  # 可改为 0.20 更宽松
```

## 新增字段说明

在推理结果中会包含这些新字段：
- `detection_reason` - 具体的判断原因
- `aggressive_score` - 激进分数（用于识别低分图）
- `is_confusing_image` - 是否困惑图像
- `model_confidence` - 模型整体确定性

## 文件变更清单

1. ✅ `src/inference.py` - 激进v2判断逻辑
2. ✅ `app.py` - 更新UI显示所有新字段
3. ✅ `test_improved_detection.py` - 详细的测试输出

## 对比总结

| 方面 | v1版本 | v2激进版本 |
|-----|--------|----------|
| 最低AI分数阈值 | 65% | 35% |
| AI漏报率 | 50% | 预期 < 20% |
| 困惑检测 | 无 | 有 |
| 综合判断 | 简单 | 多方位 |

## 下一步

1. **立即测试** - 用您的4张AI图片验证效果
2. **根据结果反馈** - 如果需要继续调整参数
3. **模型优化**（可选） - 如果效果仍不理想，可以考虑重新训练模型
