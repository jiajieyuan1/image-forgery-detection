# AI图片检测算法 v4 - 平衡版本（完全重设）

## 问题根源分析 ❌

之前的v2/v3版本导致准确率只有50%：
- AI图片4张：判对2张，误判2张为真实
- 真实图片4张：判对2张，误判2张为AI
- **症状**：算法太激进，导致严重过拟合分类边界

## v4 平衡版本设计原理

采用**分层保守策略**，优先准确性而非检测率：

### 判断标准表

| 优先级 | 判断条件 | 判定结果 | 置信度 | 说明 |
|-------|--------|--------|--------|------|
| 1️⃣ **极高** | fake ≥ 75% | 🚨 AI | 高 | 模型非常确定 |
| 2️⃣ **极高** | real ≥ 75% | ✅ 真实 | 高 | 模型非常确定 |
| 3️⃣ **中等** | fake ≥ 55% + 差距 ≥ 15% | ⚠️ AI | 中 | 足够的分离度 |
| 4️⃣ **中等** | real ≥ 55% + 差距 ≥ 15% | ✅ 真实 | 中 | 足够的分离度 |
| 5️⃣ **低** | fake ≥ 55% | ? AI | 低 | 信号弱 |
| 6️⃣ **低** | real ≥ 55% | ✅ 真实 | 低 | 信号弱 |
| 7️⃣ **无** | ~ 50/50 | ✅ 真实（默认） | 极低 | 无法分辨→保守 |

### 关键改进点

#### 1. **极高置信度阈值 (75%)**
```
原来: fake >= 35% → 判AI (太激进，导致真实误判)
现在: fake >= 75% → 判AI (只有极确定才判)
```
**效果**：避免低分数的边界情况误判

#### 2. **概率差距要求**
```
中等判断时必须满足:
- fake >= 55% AND 差距 >= 15%  OR
- real >= 55% AND 差距 >= 15%
```
**效果**：确保两类之间有足够分离，不会因为tiny差异误判

#### 3. **保守默认策略**
```
当接近 50/50 时 → 判真实（而非AI）
原因: 宁可漏报AI，不可误报真实
```

### 新增诊断指标

| 指标 | 含义 | 应用 |
|-----|------|------|
| `fake_real_ratio` | 假/真概率比例 | > 1表示AI倾向，== 1表示无差异 |
| `entropy` | 信息熵（不确定性） | > 0.69意味着接近50/50 |
| `confidence_level` | High/Medium/Low/Very Low | 判断可信度 |
| `probability_difference` | 两类差距 | >= 15%才认为足够分离 |

## 参数说明

### 可调整的关键参数

```python
# 极高置信度阈值（保守）
THRESHOLD_VERY_HIGH = 0.75  # 不建议改

# 中等置信度阈值
THRESHOLD_MEDIUM = 0.55  # 可改为 0.50-0.60

# 概率差距要求
MIN_PROB_DIFF = 0.15  # 可改为 0.10-0.20，数值越大越保守

# 概率不确定性阈值
ENTROPY_UNCERTAIN = 0.69  # log(2), 不建议改
```

### 调参建议

如果测试后仍有误判，按优先级调整：

**如果误判太多AI为真实**（不够敏感）：
```python
THRESHOLD_MEDIUM = 0.50  # 降低中等阈值
MIN_PROB_DIFF = 0.10     # 降低差距要求  
```

**如果误判太多真实为AI**（太敏感）：
```python
THRESHOLD_MEDIUM = 0.60  # 提高中等阈值
MIN_PROB_DIFF = 0.20     # 提高差距要求
THRESHOLD_VERY_HIGH = 0.80  # 提高极高阈值
```

## 预期效果

### vs v2/v3激进版本

| 方面 | v2/v3激进 | v4平衡 |
|-----|---------|--------|
| AI准确率 | 50% | 期望 ≥ 75% |
| 真实准确率 | 50% | 期望 ≥ 75% |
| 误判方向 | 双向误判 | 倾向保守 |
| 激进程度 | 极度激进 | 保守稳定 |

## 使用方法

### 1. 立即测试

```bash
python test_improved_detection.py --dir ai图片
```

输出示例对于一张13.5% fake_prob的AI图：
```
【检测结果】:     Fake
【检测等级】:     Possibly AI
【置信度等级】:   Low
【检测原因】:     Weak AI signal (51-55%, uncertain)
真实概率:         86.50%
假图概率:         13.50%
概率差距:         73.00% ✓（充足）
假/真比例:        0.156
概率熵:           0.52
```

### 2. 启动Web界面

```bash
streamlit run app.py
```

### 3. 快速推理

```bash
python quick_infer.py <image_path>
```

## 重要说明

- ✅ 新算法**更保守**，宁可漏报也不误报
- ✅ 使用**绝对阈值**而非复杂的激进计算
- ✅ 置信度等级清晰，便于理解
- ⚠️ 第一次测试后**必须验证准确率**
- ⚠️ 如果还是不理想，需要收集数据重新训练模型

## 文件修改清单

1. ✅ `src/inference.py` - 完全重设v4算法
2. ✅ `test_improved_detection.py` - 显示新指标
3. ✅ 本文档 - `ALGORITHM_V4_BALANCED.md`

## 下一步行动

1. **运行测试**：`python test_improved_detection.py --dir ai图片`
2. **记录结果**：统计真实/AI的准确率
3. **反馈**：告诉我准确率，如需调整参数再修改
4. **如果 ≥ 75%**：算法OK，可部署
5. **如果 < 75%**：需要进一步调整或考虑重训模型
